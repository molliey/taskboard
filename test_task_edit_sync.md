# Task Edit 实时同步测试指南

## 🧪 **测试环境准备**

### 1. 启动后端服务
```bash
cd backend
uvicorn main:app --reload
```

### 2. 启动前端服务
```bash
cd frontend
npm run dev
```

### 3. 启动Redis
```bash
docker run --name taskboard-redis -p 6379:6379 -d redis:7
```

## 🎯 **功能测试步骤**

### 测试1: 基础Task更新同步
**目标**: 验证task编辑后其他用户能实时看到变化

**步骤**:
1. 打开两个浏览器窗口（Chrome + Safari 或两个无痕窗口）
2. 用不同邮箱注册/登录两个用户账号
3. 创建一个共同的项目，并添加彼此为成员
4. 用户A编辑一个任务的标题或描述
5. 观察用户B的界面是否立即更新

**预期结果**: 
✅ 用户B应该立即看到任务的变化，无需刷新页面

### 测试2: 编辑冲突检测
**目标**: 验证编辑状态广播和冲突警告

**步骤**:
1. 用户A双击任务进入编辑模式
2. 观察用户B界面上该任务是否显示"用户A正在编辑"指示器
3. 用户B尝试编辑同一任务
4. 检查是否弹出冲突警告对话框

**预期结果**:
✅ 用户B看到橙色边框和编辑指示器
✅ 用户B点击编辑时弹出确认对话框
✅ 对话框显示"用户A正在编辑此任务，是否继续编辑？"

### 测试3: 编辑状态清理
**目标**: 验证编辑完成后状态正确清理

**步骤**:
1. 用户A编辑任务并保存
2. 检查用户B界面上编辑指示器是否消失
3. 用户A编辑任务但取消编辑
4. 检查指示器是否同样消失

**预期结果**:
✅ 保存/取消后编辑指示器立即消失
✅ 任务卡片恢复正常样式

### 测试4: 断线重连测试
**目标**: 验证网络问题时的状态处理

**步骤**:
1. 用户A进入编辑模式
2. 关闭用户A的浏览器标签页（模拟断线）
3. 检查用户B界面上编辑指示器是否自动清理

**预期结果**:
✅ 用户A断线后，编辑指示器在用户B界面消失

## 🔍 **调试检查点**

### 浏览器开发者工具检查
1. **Network标签**: 检查WebSocket连接状态
2. **Console标签**: 查看实时消息日志
3. **Application标签**: 检查localStorage中的用户信息

### 预期的Console日志
```javascript
// 编辑开始时
WebSocket connected successfully
Sent WebSocket message: task_edit_start {taskId: "task-123", userId: "1", userName: "Alice"}

// 其他用户收到
Received WebSocket message: task_edit_start
User Alice is editing task-123

// 编辑结束时
Sent WebSocket message: task_edit_end {taskId: "task-123", userId: "1"}
```

## 🚨 **常见问题排查**

### 问题1: WebSocket连接失败
**解决**: 
- 检查后端是否运行在 localhost:8000
- 确认Redis服务正常运行
- 检查防火墙设置

### 问题2: 编辑指示器不显示
**检查**:
- 浏览器Console是否有错误
- 用户是否成功登录
- 项目成员关系是否正确

### 问题3: 更新不同步
**检查**:
- task_updated事件是否正常发送
- Board组件是否正确监听事件
- 数据格式是否匹配

## ✅ **测试通过标准**

全功能正常的标准:
1. ✅ Task编辑实时同步
2. ✅ 编辑冲突检测和警告
3. ✅ 编辑状态视觉指示器
4. ✅ 用户名正确显示
5. ✅ 编辑状态自动清理
6. ✅ 断线状态处理

## 🎉 **成功指标**

如果以上测试全部通过，说明Task Edit实时同步功能已完整实现，用户可以享受流畅的多人协作编辑体验！